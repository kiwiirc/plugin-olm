import { TAGS } from '../constants'

// eslint-disable-next-line import/prefer-default-export
export const FRAGMENTABLE_TAGS = [
	TAGS.OLM_PACKET,
	TAGS.OLM_IDENTITY,
	TAGS.OLM_ONETIMEKEY,
	TAGS.MEGOLM_STATE,
	TAGS.MEGOLM_PACKET,
]
